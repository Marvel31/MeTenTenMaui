# MeTenTen v1.2 마이그레이션 가이드

## 🔐 암호화 기능 추가

v1.2부터는 사용자의 10&10 내용이 AES-256 암호화되어 Firebase에 저장됩니다.

## ⚠️ 중요: 기존 데이터에 대한 안내

### 기존 평문 데이터 처리

v1.1.0 이하 버전에서 작성한 10&10 데이터는 **평문(암호화되지 않음)**으로 Firebase에 저장되어 있습니다.

v1.2로 업데이트 후:
- **신규로 작성하는 모든 10&10은 자동으로 암호화됩니다**
- **기존 평문 데이터는 암호화되지 않은 상태로 유지됩니다**
- 기존 평문 데이터를 수정하면 암호화되어 저장됩니다

### 권장 사항

보안을 위해 다음 중 하나를 선택하시기 바랍니다:

#### 옵션 1: 기존 데이터 삭제 (권장)
1. v1.2 업데이트 전에 필요한 데이터를 백업
2. Firebase Console에서 기존 데이터 삭제
3. v1.2로 업데이트 후 새로 작성

#### 옵션 2: 기존 데이터 유지
- 기존 평문 데이터를 읽을 수 있습니다
- 보안이 중요한 내용은 다시 작성하여 암호화하세요

#### 옵션 3: 수동 마이그레이션
1. 앱에서 기존 10&10을 하나씩 열어서 내용 복사
2. 삭제 후 다시 작성 (자동으로 암호화됨)

---

## 🔑 암호화 작동 방식

### 키 생성
- 사용자 이메일 + 비밀번호를 사용하여 PBKDF2로 암호화 키 생성
- 100,000 iterations, SHA-256 해시 사용

### 암호화 알고리즘
- AES-256-CBC 암호화
- 각 데이터마다 랜덤 IV(Initialization Vector) 사용
- Base64로 인코딩하여 Firebase에 저장

### 보안 특성
- Firebase 관리자도 암호화된 내용을 읽을 수 없음
- 사용자의 비밀번호를 알아야만 복호화 가능
- 로그아웃 시 메모리에서 암호화 키 자동 삭제

---

## ⚠️ 주의 사항

### 비밀번호 관리
- **비밀번호를 잊어버리면 데이터를 복구할 수 없습니다**
- 비밀번호는 안전한 곳에 별도로 기록해두세요
- 비밀번호 변경 시 기존 암호화된 데이터는 복호화할 수 없게 됩니다

### 데이터 백업
- 중요한 내용은 별도로 백업하세요
- Firebase Console에서도 암호화된 데이터는 읽을 수 없습니다

### 계정 공유 금지
- 암호화 키는 이메일 + 비밀번호로 생성됩니다
- 다른 사람과 계정을 공유하지 마세요

---

## 🔧 문제 해결

### "[복호화 실패]" 메시지가 표시됨
- **원인**: 잘못된 비밀번호로 로그인했거나 데이터가 손상됨
- **해결**: 
  1. 로그아웃 후 올바른 비밀번호로 다시 로그인
  2. 문제가 계속되면 해당 10&10을 삭제하고 다시 작성

### 기존 데이터를 읽을 수 없음
- **원인**: v1.1.0 이하에서 작성한 평문 데이터는 IsEncrypted=false로 표시되어야 함
- **해결**: Firebase Console에서 해당 데이터의 `isEncrypted` 필드를 `false`로 설정

### 비밀번호를 잊어버렸음
- **결과**: 암호화된 데이터를 복구할 수 없습니다
- **대응**: 
  1. Firebase Authentication에서 비밀번호 재설정
  2. 기존 암호화 데이터는 포기
  3. 새로 작성하는 데이터는 새 비밀번호로 암호화됨

---

## 📝 개발자 참고

### Firebase 데이터 구조
```json
{
  "tentens": {
    "userId123": {
      "tentenId456": {
        "content": "base64EncodedEncryptedData...",
        "isEncrypted": true,
        "topicId": "1",
        "createdAt": "2025-01-12T10:30:00.000Z",
        "updatedAt": "2025-01-12T11:45:00.000Z"
      }
    }
  }
}
```

### 암호화 키 생성 코드 예시
```csharp
// PBKDF2를 사용한 키 생성
var salt = Encoding.UTF8.GetBytes(email + "MeTenTen2025");
using (var pbkdf2 = new Rfc2898DeriveBytes(password, salt, 100000, HashAlgorithmName.SHA256))
{
    _key = pbkdf2.GetBytes(32); // 256-bit key
}
```

---

## 📞 지원

문제가 지속되면:
- GitHub Issues에 버그 리포트
- 로그 파일 첨부 (민감한 정보 제거 후)
- Firebase Console에서 데이터 확인

**MeTenTen v1.2.0**  
**릴리스 날짜**: 2025-01-12  
**암호화로 더욱 안전하게 📝🔐**

